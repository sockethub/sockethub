{
  "required": ["type"],
  "properties": {
    "type": {
      "enum": [
        "connect",
        "update",
        "join",
        "leave",
        "send",
        "query",
        "announce",
        "disconnect"
      ]
    }
  }
}
