{
  "required": ["type"],
  "properties": {
    "type": {
      "enum": [
        "connect",
        "join",
        "leave",
        "send",
        "update",
        "request-friend",
        "remove-friend",
        "make-friend",
        "query",
        "disconnect"
      ]
    }
  }
}
