language: node_js
os: linux
dist: focal
arch: arm64
node_js:
  - 'lts/*'
  - 15
services: redis
sudo: false
install:
  - npm update npm -g && npm install -g lerna yarn && yarn run install
script:
  - yarn run travis