language: node_js
node_js:
  - 10
  - 12
  - 13
services: redis
sudo: false
script:
  - lerna run test
install:
  - npm update npm -g && npm install -g lerna && lerna bootstrap
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
