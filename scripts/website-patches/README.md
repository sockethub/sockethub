# website-sockethub.org IRC room naming docs

Patches for `fix/irc-room-naming-docs-2be9` on `sockethub/website-sockethub.org`.

Apply from a website-sockethub.org checkout:

```bash
git checkout master && git pull
git checkout -b fix/irc-room-naming-docs-2be9
git am scripts/website-patches/*.patch   # from sockethub repo, or copy patches locally
npm run build
git push -u origin fix/irc-room-naming-docs-2be9
```
